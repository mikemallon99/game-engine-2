todo:
- FPS movement DONE
- collisions (AABB) DONE
- jumping DONE
- up/down movement DONE
- gui elements
- level editor
    - add in cubes
    - resize cubes
    - modal that can be dragged around
    - add in individual faces
- sounds
- models that arent cubes
- collisions (SAT)
    - rotated cubes or nonstandard geometry
- fps movement sway
- flashlight decays with distance
- skybox


sequence:
1. gui elements
    a. 2d stuff at all
    b. text
    c. minimal debug menu: boxes with text
    d. select from 3 textures with debug menu
2. add in custom geometry in code
2. add in geometry with gui (temp)
3. save geometry to file and reload
4. drag geometry to different points on grid using gimbal
5. resize geometry using gimbal
6. change texture of geometry

problem: need to get text working

custom geometry:
- place a face somewhere
- move face around
- move vertices around 
- add in new faces
- add in collisions
- custom textures
- move things around in the engine

hit right to select, then do up/down
face_edit
- edit_verts
- translate
- scale
- rotate

- vertex: 1 +/-
increment/decrement by 0.1
- x:
- y:
- z:

- scale: 
- x:
- y:
- z:

edit faces
spawn new face

FACE 1:
- translate
    - X: 1 (+/- 1)
    - Y: -2 (+/- 1)
    - Z: 0 (+/- 1)

- edit vertices
    - 0
        - X: -0.5 (+/- 0.1)
        - Y: -0.5 (+/- 0.1)
        - Z: -0.5 (+/- 0.1)
    - 1
    - 2
    - 3

- scale
    - X
    - Y 
    - Z
- rotate

FACE 2:


TASK: ADD IN A CUSTOM MAP
- would be cool to have a map on a street
- add in textures like grass, sidewalk, fences, etc

task breakdown:
- to make the map i need to use blender
    - need to use blender -> opengl export pipeline
- start by making one model and load it in the game
- make a custom map, set collisions as the floor plane
- write collision system for complex meshes, not AABB

make a map in blender
load map in the game in place of the cube
have the collisions be a plane 
texture the map, load in textures 
rewrite collisions to work with the map

list of stuff:
- ability to select map
- bug with texture select (done)
- make a better map (done)
- make a skybox  (done)
- make collisions into a cylinder
- add in walk and jump sounds

object selection system:
- function that can cast a ray and see if it hits an object 
- object selection system which can select different types of objects (AABB, model)
- function that can project the mouses ray direction onto an XYZ axis
- geometry shader for xyz modals + xyz axes that spawns it on the current axis
- fragment shader which outlines the currently selected object in orange
- function that casts a ray to select a model on the currently selected object

object modal system:
- put an AABB box around each gizmo axis
- if a bbox is selected and user presses down on mouse, then project their mouse movements onto that axis

selection:
- need to make it so you can only select one thing at a time. should be the first thing that is intersected
- make it so that if you intersect the ray behind you, then it doesnt select anything
- if you dont select another bounding box, but say you selected the gizmo thats owned by the bounding box, then it doesnt deselect anything
    - groups? bbox is the parent, gizmo is a child
- need to improve the equation for translating an object along the gizmo

next directions to take:
- add in animations
    - character model onto stage
    - rigging
    - play animation
- save edits to stage
    - serialize the stage to a file
        - USD?
- swap out bbox selection with object orange outline

components of battle system:
- need a GUI to select the gag
- need an XP system for getting points
- animations for attacking
- player gag inventory
- health system
- damage system, accuracy
- different states for each type of gag
    - throw/squirt = medium/high acc single damage
    - sound = area damage
    - lure = stun + double damage
    - trap = damage only if lured
    - drop = low accuracy single damage
    - toonup = not needed since this is single player
- levels of gags for each track
- grid selection system 
- way to walk up to an NPC to initiate a battle

where does battle data get tracked?
- as a test we could get away with tracking everything in one spot
- player health (only ever one player)
- enemy health (there are many possible enemies)
- current gags
- current level of gag, XP level

on any given frame, there might be a battle active
a battle is a state machine that goes back and forth between the players and the cogs

battle
- run loop updates battle
    - battle updates based off state machine
- render loop draws battle if its active
- battle controller
    - player can select a gag with mouse


game engine:
- run loop
    - process inputs
    - update game logic (first half)
    - draw calls (second half)

current architecture:
- lots of global variables in the program & variables that are local to the run loop
    globals:
        - logger
        - stage
        - debug menu
        - debounced keyboard mgr
        - camera
    locals:
        - shaders
        - models
        - light cube VBO/VAO/positions
        - texture IDs
        - animator + animation for model
        - collision objects
- game logic update loop:
    - controller update stuff
        - keyboard debounce manager update
        - set player move direction, calc move vector based off camera
        - enable/disable flashlight
        - fart sound/logging
        - player jumping
        - debug menu controller update
            - go up/down on menu
            - select item or go back
            - inc/dec value
        - stage controller update
            - activate/deactivate move gizmo + model outline based on mouse click
            - set move gizmo selection, set translate xform
    - calculate player move vector 
        - cams move vector
        - gravity move vector
        - update move vector based on collisions
    - check if player is hitting the floor, cancel y velocity if so
    - play sound if the player moves
    - update NPC animation 
    - update stage
        - see if player is hovering over any models
        - see if player is hovering / moving gizmo
- callback functions
    - mouse & scroll callbacks for moving camera


how to handle inputs? should we use MVC framework?
- model updates every frame
- controller sets values in a state machine that the model reads on every update
- view is the renderer that runs every frame based off the model


other stuff to implement:
- doors that take you to new areas
- billboard items to get health
- on screen log that can easily be written to
    - scrolls just like a chat log
    - log.output(string)


